{
  "name": "sequelize-mysql",
  "version": "1.0.0",
  "description": "",
  "main": "dist/server.js",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "postinstall": "chmod +x ./node_modules/.bin/tsup",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon --watch 'src/**/*' --ext ts,json --exec tsx src/server.ts",
    "build": "npx tsup src/server.ts --format cjs --dts --clean",
    "start": "node dist/server.js",
    "build:dev": "npm run build && npm run start",
    "watch": "nodemon --watch 'src/**/*' --ext ts,json --exec npm run build:dev",
    "type-check": "tsc --noEmit"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@fastify/cors": "^11.1.0",
    "@fastify/helmet": "^13.0.1",
    "@fastify/swagger": "^9.5.1",
    "@fastify/swagger-ui": "^5.2.3",
    "@supabase/supabase-js": "^2.57.4",
    "dotenv": "^17.2.2",
    "fastify": "^5.6.0",
    "helmet": "^8.1.0",
    "nodemon": "^3.1.10",
    "tsup": "^8.5.0",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2"
  },
  "devDependencies": {
    "@types/node": "^24.3.2"
  }
}
